{% extends 'base.html.twig' %}

{% block title %}Reset password{% endblock %}

{% block body %}
    {% if (etape == 2) %}
        {{ include('form/formpassword.html.twig') }}
    {% else %}
        {{ include ('form/formresetpassword.html.twig') }}
    {% endif %}
{% endblock %}